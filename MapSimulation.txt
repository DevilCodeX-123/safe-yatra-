import React from "react";
import { MapPin, Navigation } from "lucide-react";
import { motion } from "framer-motion";

export default function MapSimulation({ source, destination, darkMode }) {
  return (
    <div className={`w-full h-full relative overflow-hidden ${
      darkMode ? 'bg-gray-900' : 'bg-gradient-to-br from-blue-100 via-green-50 to-blue-50'
    }`}>
      {/* Animated route line */}
      <motion.div
        initial={{ pathLength: 0 }}
        animate={{ pathLength: 1 }}
        transition={{ duration: 2, ease: "easeInOut" }}
        className="absolute inset-0"
      >
        <svg className="w-full h-full" viewBox="0 0 400 300">
          <motion.path
            d="M 50 250 Q 150 150, 200 150 T 350 50"
            stroke={darkMode ? "#3B82F6" : "#2563EB"}
            strokeWidth="4"
            fill="none"
            strokeDasharray="10,5"
            initial={{ pathLength: 0 }}
            animate={{ pathLength: 1 }}
            transition={{ duration: 2 }}
          />
        </svg>
      </motion.div>

      {/* Source marker */}
      <motion.div
        initial={{ scale: 0 }}
        animate={{ scale: 1 }}
        transition={{ delay: 0.5 }}
        className="absolute left-12 bottom-12"
      >
        <div className={`w-12 h-12 rounded-full flex items-center justify-center ${
          darkMode ? 'bg-green-600' : 'bg-green-500'
        } shadow-lg`}>
          <MapPin className="w-7 h-7 text-white" />
        </div>
        <div className={`mt-2 px-3 py-1 rounded-lg text-sm font-medium ${
          darkMode ? 'bg-gray-800 text-white' : 'bg-white text-gray-900'
        } shadow`}>
          {source}
        </div>
      </motion.div>

      {/* Destination marker */}
      <motion.div
        initial={{ scale: 0 }}
        animate={{ scale: 1 }}
        transition={{ delay: 1 }}
        className="absolute right-12 top-12"
      >
        <div className={`w-12 h-12 rounded-full flex items-center justify-center ${
          darkMode ? 'bg-red-600' : 'bg-red-500'
        } shadow-lg`}>
          <MapPin className="w-7 h-7 text-white" />
        </div>
        <div className={`mt-2 px-3 py-1 rounded-lg text-sm font-medium ${
          darkMode ? 'bg-gray-800 text-white' : 'bg-white text-gray-900'
        } shadow`}>
          {destination}
        </div>
      </motion.div>

      {/* Animated vehicle */}
      <motion.div
        initial={{ offsetDistance: "0%" }}
        animate={{ offsetDistance: "100%" }}
        transition={{ duration: 3, repeat: Infinity, ease: "linear" }}
        className="absolute"
        style={{
          offsetPath: "path('M 50 250 Q 150 150, 200 150 T 350 50')",
          offsetRotate: "0deg"
        }}
      >
        <div className={`w-8 h-8 rounded-full flex items-center justify-center ${
          darkMode ? 'bg-blue-600' : 'bg-blue-500'
        } shadow-lg`}>
          <Navigation className="w-5 h-5 text-white" />
        </div>
      </motion.div>

      {/* Grid overlay */}
      <div className={`absolute inset-0 opacity-10 ${darkMode ? 'opacity-5' : ''}`}
        style={{
          backgroundImage: `linear-gradient(${darkMode ? '#fff' : '#000'} 1px, transparent 1px), linear-gradient(90deg, ${darkMode ? '#fff' : '#000'} 1px, transparent 1px)`,
          backgroundSize: '40px 40px'
        }}
      />
    </div>
  );
}
